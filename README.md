# Authentication-and-authorisation
Coder Academy Activity: Create a global state to handle authenticated user info.

Successfully captured user info in the frontend, pass that info to the backend, create a jwt, return the jwt to the frontend and then, use that jwt to access a protected route.

Set up local Storgae.